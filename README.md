# Supertokens Microsoft Entra ID OIDC

This project is configured to use Microsoft Entra ID as a custom provider.

In order to work, you must add the following to backend/.env

```
CLIENT_ID=
CLIENT_SECRET=
```

Simply

`npm install`
`npm run start`